# 2 — Guide: MCP enablement in IDE UI line (conditional)

## Goal
If phase 1 leaves only the skill as the lone source without a grounding line in `guide.md`, add a short bullet under **IDE wiring checklist** (`extsrc/files/.mempalace/guides/guide.md`): after merge, enable MCP/servers in the IDE UI whenever the client requires it.

## Approach
- One or two sentences; avoid duplicating the full checklist body.
- Run this phase **only if** whoever implements concludes the skill alone is not enough (e.g. users read `guide.md` without the skill).

## Affected files
- `extsrc/files/.mempalace/guides/guide.md` *(optional)*

## Constraints
Do not bloat `guide.md`; do not alter MCP JSON or add product-specific screenshots.
